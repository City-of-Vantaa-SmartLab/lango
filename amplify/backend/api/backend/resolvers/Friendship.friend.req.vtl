#set($statement1 = "
  SELECT *
  FROM users
  WHERE
    id IN ('$context.source.initiatorUserId', '$context.source.otherUserId')
    AND id != get_user_id_by_cognito_id('$context.identity.cognitoIdentityId')
")

{
  "version": "2018-05-29",
  "statements": [
    $utils.toJson($statement1)
  ]
}
